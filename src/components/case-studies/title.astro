---
import Cover from "@components/global/cover.astro";
const base = import.meta.env.BASE_URL;

const coverConfig = {
  title: "Casos de estudio",
  background: `linear-gradient(rgba(0, 45, 100, 0.65), rgba(0, 45, 100, 0.65)), url("${base}assets/images/resources-banner-image.jpg") !important; `,
};
---

<section class="cover">
  <Cover config={coverConfig} />
</section>
